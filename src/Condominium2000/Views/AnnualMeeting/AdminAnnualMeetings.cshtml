@using Condominium2000.Helpers
@using Condominium2000.Resources
@using Condominium2000.Resources.AnnualMeeting
@model List<Condominium2000.Models.AnnualMeeting>

@{
    ViewBag.Title = AdminAnnualMeetings.VIEW_BAG_TITLE;
}

<div id="content_margin_top"></div>
<div id="content_center" class="center">
    <div id="content_top">
        <div id="content_margin_left_xl"></div>
        <div id="content_full">
            <div class="default_info">
                <p class="default_info">
                    @AdminAnnualMeetings.TEXT_HELP_1
                </p>

                <br/>
                <fieldset>
                    <legend>@AdminAnnualMeetings.TEXT_ADMIN_ANNUAL_MEETING</legend>
                    <br/>
                    <div class="default_content">
                        <table class="admin_table">
                            <tr class="admin_table_header">
                                <td>
                                    @AdminAnnualMeetings.TABLE_NAME
                                </td>
                                <td>
                                    @AdminAnnualMeetings.TABLE_TYPE
                                </td>
                                <td>
                                    @AdminAnnualMeetings.TABLE_SIZE
                                </td>
                                <td></td>
                                <td></td>
                            </tr>

                            @* Var even: switch between even and odd table layout *@
                            @{ var IsEven = true; }

                            @foreach (var annualMeeting in Model)
                            {
                                @* Start tag *@
                                @Html.SwitchTable("admin_table_even", "admin_table_odd", ref IsEven)
                                <td>
                                    @Html.ReturnLanguageContent(annualMeeting, TableContent.Name)
                                </td>
                                <td>
                                    @annualMeeting.FileType
                                </td>
                                <td>
                                    @FileHelper.DisplaySize(annualMeeting.FileSize)
                                </td>
                                <td>
                                    @Html.ActionLink(AdminAnnualMeetings.BUTTON_EDIT_ANNUAL_MEETING, "Edit", new {id = annualMeeting.Id}, new {@class = "button_default"})
                                </td>
                                <td>
                                    @Html.ActionLink(AdminAnnualMeetings.BUTTON_DELETE_ANNUAL_MEETING, "Delete", new {id = annualMeeting.Id}, new {@class = "button_default"})
                                </td>

                                @* Closing tag *@
                                @:</tr>
                            }
                        </table>
                        <br/><br/>
                        <p>
                            @Html.ActionLink(AdminAnnualMeetings.BUTTON_CREATE_ANNUAL_MEETING, "Create", new {controller = "AnnualMeeting"}, new {@class = "button_default"})
                        </p>
                        <br/>
                    </div>
                </fieldset>
            </div>
        </div>
    </div>
    <div id="content_bottom" class="side_border">
        <div id="bottom_fast_links" class="quick_navigation">
            <div id="fl_divider">
                <div class="fl_divider"></div>
            </div>
            <div id="fl_space"></div>
            <div id="fl_news">
                @Html.ActionLinkBack(ContentBottom.BACK)
            </div>
            @Html.ActionLinkTop(ContentBottom.TOP)
        </div>
    </div>
</div>