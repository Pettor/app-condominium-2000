@using Condominium2000.Helpers
@using Condominium2000.Resources
@using Condominium2000.Resources.AnnualReport
@model List<Condominium2000.Models.AnnualReport>

@{
    ViewBag.Title = AdminAnnualReports.VIEW_BAG_TITLE;
}

<div id="content_margin_top"></div>
<div id="content_center" class="center">
    <div id="content_top">
        <div id="content_margin_left_xl"></div>
        <div id="content_full">
            <div class="default_info">
                <p class="default_info">
                    @AdminAnnualReports.TEXT_HELP_1
                </p>

                <br/>
                <fieldset>
                    <legend>@AdminAnnualReports.TEXT_ADMIN_ANNUAL_REPORT</legend>
                    <br/>
                    <div class="default_content">
                        <table class="admin_table">
                            <tr class="admin_table_header">
                                <td>
                                    @AdminAnnualReports.TABLE_NAME
                                </td>
                                <td>
                                    @AdminAnnualReports.TABLE_TYPE
                                </td>
                                <td>
                                    @AdminAnnualReports.TABLE_SIZE
                                </td>
                                <td></td>
                                <td></td>
                            </tr>

                            @* Var even: switch between even and odd table layout *@
                            @{ var IsEven = true; }

                            @foreach (var annualReport in Model)
                            {
                                @* Start tag *@
                                @Html.SwitchTable("admin_table_even", "admin_table_odd", ref IsEven)
                                <td>
                                    @Html.ReturnLanguageContent(annualReport, TableContent.Name)
                                </td>
                                <td>
                                    @annualReport.FileType
                                </td>
                                <td>
                                    @FileHelper.DisplaySize(annualReport.FileSize)
                                </td>
                                <td>
                                    @Html.ActionLink(AdminAnnualReports.BUTTON_EDIT_ANNUAL_REPORT, "Edit", new {id = annualReport.Id}, new {@class = "button_default"})
                                </td>
                                <td>
                                    @Html.ActionLink(AdminAnnualReports.BUTTON_DELETE_ANNUAL_REPORT, "Delete", new {id = annualReport.Id}, new {@class = "button_default"})
                                </td>

                                @* Closing tag *@
                                @:</tr>
                            }
                        </table>
                        <br/><br/>
                        <p>
                            @Html.ActionLink(AdminAnnualReports.BUTTON_CREATE_ANNUAL_REPORT, "Create", new {controller = "AnnualReport"}, new {@class = "button_default"})
                        </p>
                        <br/>
                    </div>
                </fieldset>
            </div>
        </div>
    </div>
    <div id="content_bottom" class="side_border">
        <div id="bottom_fast_links" class="quick_navigation">
            <div id="fl_divider">
                <div class="fl_divider"></div>
            </div>
            <div id="fl_space"></div>
            <div id="fl_news">
                @Html.ActionLinkBack(ContentBottom.BACK)
            </div>
            @Html.ActionLinkTop(ContentBottom.TOP)
        </div>
    </div>
</div>